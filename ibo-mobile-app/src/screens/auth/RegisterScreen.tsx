/**
 * RegisterScreen is no longer used directly.
 * Login + Sign Up are both handled inside LoginScreen via tab state.
 * This file is kept to satisfy any stale imports but simply re-exports LoginScreen.
 */
export { default } from './LoginScreen';
